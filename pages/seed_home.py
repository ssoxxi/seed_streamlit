# =============================================================================
# 홈 페이지 (Home Page)
# =============================================================================

import streamlit as st
from theme import apply_theme # 앱 theme
apply_theme()
st.set_page_config(page_title="홈 | SEED 팀 최종 프로젝트 개요", layout="wide", page_icon="🏠")
st.title("🌱 SEED 팀 최종 프로젝트 개요")

# =============================================================================
# 사이드바: 환영 메시지 (main.py의 placeholder 활용)
# =============================================================================
with st.session_state.sidebar_placeholder.container():
    st.header("👋 환영합니다!")
    st.write("사이드바에서 페이지를 선택하세요.")

# =============================================================================
# 앱 소개
# =============================================================================
st.header("Streamlit 스타트업 투자전략 추천시스템")

st.markdown("""
이 앱은 Streamlit으로 구현한 스타트업 투자전략 추천시스템입니다.
### 내용
- 데이터 분석을 통해 도출한 결과 및 인사이트 제시
- ML을 통한 스타트업 투자 포트폴리오 추천 시스템
""")

# =============================================================================
# 앱 구조 설명
# =============================================================================
st.divider()
st.header("📁 앱 구조")

col1, col2 = st.columns(2)

with col1:
    st.subheader("파일 구조")
    st.code("""
03_multipage_app/
├── seed_main.py              # 엔트리 포인트 (진입점)
└── pages/
    ├── seed_home.py            # 홈 페이지 (현재)
    └── recommendation.py       # 투자전략 시뮬레이션 및 추천 페이지
    """, language="text")

with col2:
    st.subheader("핵심 코드 (seed_main.py)")
    st.code("""
import streamlit as st

# 페이지 정의
home_page = st.Page("pages/seed_home.py", title="홈", icon="🌱", default=True)
data_screening = st.Page("pages/screening.py", title="스타트업 스크리닝", icon="📊"))
recommend_page = st.Page("pages/recommendation.py", title="투자전략 시뮬레이션 및 추천", icon="💰")

# 네비게이션 구성 (딕셔너리로 섹션 구분)
pg = st.navigation({
    "메인": [home_page],
    "분석": [data_screening],
    "추천": [recommend_page]
})

# 공통 사이드바 요소
with st.sidebar:
    st.caption("© 2025 [SPARTA x SEED] Final Project App")

# 선택된 페이지 실행 (필수!)
pg.run()
    """, language="python")

# =============================================================================
# 페이지 소개 카드
# =============================================================================
st.divider()
st.header("📑 페이지 소개")

col1, col2 = st.columns(2)

with col1:
    with st.container(border=True, height=280):
        st.subheader("📊 스타트업 스크리닝")
        st.write("기업에 대한 데이터를 스크리닝 합니다. ")

with col2:
    with st.container(border=True, height=280):
        st.subheader("💰 투자 전략 시뮬레이션 및 추천")
        st.write("다양한 투자 전략을 시뮬레이션하고 VC를 위한 투자전략을 추천합니다.")

# =============================================================================
# Seed팀 소개
# =============================================================================
st.divider()
st.header("🔄 SEED팀 소개")

st.info("""
**SEED**는 Startup Equity & Exit Data에서 따온 약자로,
스타트업의 지분(Equity)과 엑시트(Exit) 데이터를 다루는 팀입니다. 

- 팀구성: 배수아(팀장), 한소희, 홍채연, 황재규 (총 4명)
- 프로젝트 기간: 2025.11.24 ~ 2025.12.30
- 모든 것이 끝나고 웃으며 만나기로 약속함.
""")

# =============================================================================
# 시작하기
# =============================================================================
st.divider()
st.success("👈 왼쪽 사이드바에서 페이지를 선택하여 시작하세요!")
